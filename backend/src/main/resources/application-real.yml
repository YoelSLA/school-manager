spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gestion_escolar_real
    username: postgres
    password: admin

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false

logging:
  level:
    org.hibernate.SQL: OFF
